# npm設定ファイル - Windows環境での依存関係問題を解決
# オプション依存関係を強制インストール
install-strategy=hoisted
# Windows用バイナリを明示的にインストール
target_platform=win32
target_arch=x64
# レジストリ設定
registry=https://registry.npmjs.org/
# キャッシュ問題を回避
cache-min=0